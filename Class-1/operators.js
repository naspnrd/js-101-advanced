// Arithmatic Operators -> +, - , *, /, %, **(Exponentiation)

//Comparison Operators -> >, <. >=, <=

// Logical Operators -> &&, ||, ! (short-circuiting)=> Condition checking, shorthand execution
let isLoggedIn = true;
let hasPermission = true;
if (isLoggedIn && hasPermission) {
} else {
}

// Relational Operators -> >, <

// Bitwise Operators -> &(Bitwise And), |(Bitwise OR), ^(Bitwise XOR), ~(Bitwise Not) -> Binary Operations, etc..

// let a = 5;
// let b = 3;

// console.log(a & b);
// console.log(a | b);
// console.log(a ^ b);
// console.log(~a);
let num = 2;
console.log((num & 1) === 0);

// Increment(++) and Decrement(--) Operators

let x = 5;
console.log(++x); // pre-increment 6
console.log(x++); // post-increment 6
console.log(x); // 7

let y = 10;

console.log(--y); // pre-decrement 9
console.log(y--); // post-decrement 9
console.log(y); // 8
